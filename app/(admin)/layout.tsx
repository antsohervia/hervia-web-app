import type { ReactNode } from "react";

export const metadata = {
  title: "Admin · TrackApp",
};

export default function AdminGroupLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
